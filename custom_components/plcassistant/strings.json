{
  "config": {
    "step": {
      "user": {
        "title": "PLCAssistant",
        "description": "Connect the thin integration to the Soft-PLC App over MQTT. Mosquitto must be installed.",
        "data": {
          "instance_id": "Instance ID (must match App option)",
          "mqtt_broker": "MQTT broker hostname",
          "mqtt_port": "MQTT broker port",
          "mock_mode": "Use mock entities (same path as field bindings)"
        }
      }
    },
    "abort": {
      "already_configured": "This instance_id is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Mock dynamics",
        "description": "Select a plant dynamics preset. Param overrides are a JSON object of numeric keys from the preset (e.g. {\"k_drain\": 6.0}). Applying rebuilds the plant from model initials.",
        "data": {
          "dynamics_preset": "Dynamics preset",
          "dynamics_params": "Param overrides (JSON object)"
        }
      }
    },
    "error": {
      "invalid_dynamics": "Invalid preset or params — check the preset name and JSON overrides"
    }
  },
  "issues": {
    "restart_required": {
      "title": "PLCAssistant: restart required",
      "description": "The App updated the thin integration on disk (running {loaded}, disk has {disk}). Restart Home Assistant Core so Soft-PLC status and entities load the new code.",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Restart Home Assistant",
            "description": "Restart Core now to load PLCAssistant {disk} (currently running {loaded})."
          }
        }
      }
    }
  }
}
