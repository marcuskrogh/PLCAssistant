# Install PLCAssistant on Home Assistant OS

The full installation guide lives in the repository root:

**→ [`../README.md`](../README.md)**

That guide covers prerequisites (HA OS + Mosquitto), adding the GitHub App repository, Ingress / port **8099**, bundling the thin integration (`custom_components/plcassistant` one-time copy), MQTT configuration, and local development.
